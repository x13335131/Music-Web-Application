@model IEnumerable<musicWebApplication.Models.Festival>

@{
    ViewBag.Title = "Index";
}

<h2>Festivals</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Dj Name
        </th>
        <th>
           Festival Name
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Location)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dj.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Location)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FestivalId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FestivalId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FestivalId })
        </td>
    </tr>
}

</table>
